

#include "ft_stckcheck.h"

int	ft_stckcheck(t_stack **stack)
{
	t_stack *temp;
	int		count;

	count = 0;
	temp = *stack;
	if (temp->index)
	while (temp->index )
	{
		
	}
	return (0);
}

int	main(void)
{

	return (0);
}
